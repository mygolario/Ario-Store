# Ario Store

یک پروژه دمو فروشگاه اینترنتی (E-commerce Demo) ساخته شده با Next.js که برای نمایش در پورتفولیو طراحی شده است.

## درباره پروژه

Ario Store یک فروشگاه اینترنتی کامل و حرفه‌ای است که برای نمایش قابلیت‌های یک پلتفرم تجارت الکترونیک مدرن طراحی شده است. این پروژه شامل تمام بخش‌های اصلی یک فروشگاه اینترنتی از جمله کاتالوگ محصولات، سبد خرید، فرآیند خرید، حساب کاربری و بلاگ است.

### ویژگی‌های اصلی

- 🛍️ **کاتالوگ کامل محصولات** - نمایش محصولات با فیلتر و مرتب‌سازی
- 🛒 **سبد خرید و فرآیند خرید** - مدیریت سبد خرید و تکمیل سفارش
- 👤 **حساب کاربری** - مدیریت پروفایل و مشاهده سفارش‌ها
- 📝 **بلاگ** - مقالات و راهنمای خرید
- 🔍 **جستجو** - جستجوی پیشرفته در محصولات
- 📱 **طراحی واکنش‌گرا** - سازگار با موبایل، تبلت و دسکتاپ
- 🌐 **پشتیبانی RTL** - رابط کاربری فارسی با چیدمان راست به چپ

## تکنولوژی‌های استفاده شده

- **Framework**: [Next.js 16](https://nextjs.org/) (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **UI Components**: shadcn/ui
- **State Management**: Zustand
- **Font**: Vazirmatn (فونت فارسی)

## ساختار پروژه

```
src/
├── app/              # صفحات و روت‌های Next.js
│   ├── account/      # بخش حساب کاربری
│   ├── blog/         # بخش بلاگ
│   ├── cart/         # سبد خرید
│   ├── checkout/     # فرآیند خرید
│   ├── product/      # صفحات محصولات
│   └── ...
├── components/       # کامپوننت‌های قابل استفاده مجدد
│   ├── cart/         # کامپوننت‌های سبد خرید
│   ├── layout/       # کامپوننت‌های لایه‌بندی
│   ├── product/      # کامپوننت‌های محصولات
│   └── ui/           # کامپوننت‌های UI پایه
├── data/             # داده‌های استاتیک (Mock Data)
│   ├── products.ts   # محصولات
│   ├── categories.ts # دسته‌بندی‌ها
│   ├── orders.ts     # سفارش‌ها
│   ├── user.ts       # اطلاعات کاربر
│   └── posts.ts      # مقالات بلاگ
├── lib/              # توابع کمکی
│   ├── products.ts   # توابع مرتبط با محصولات
│   ├── date.ts       # فرمت تاریخ فارسی
│   └── utils.ts      # توابع عمومی
└── store/            # مدیریت state با Zustand
    └── cart.ts       # استور سبد خرید
```

## نصب و راه‌اندازی

### پیش‌نیازها

- Node.js 18+ 
- npm یا yarn

### مراحل نصب

1. کلون کردن پروژه:
```bash
git clone <repository-url>
cd Ario-Store
```

2. نصب وابستگی‌ها:
```bash
npm install
```

3. اجرای پروژه در حالت توسعه:
```bash
npm run dev
```

4. باز کردن مرورگر:
```
http://localhost:3000
```

### ساخت نسخه Production

```bash
npm run build
npm start
```

## صفحات اصلی

- `/` - صفحه اصلی با محصولات ویژه و بخش‌های بازاریابی
- `/products` - لیست تمام محصولات با فیلتر و مرتب‌سازی
- `/product/[slug]` - صفحه جزئیات محصول
- `/category/[slug]` - محصولات یک دسته‌بندی خاص
- `/cart` - سبد خرید
- `/checkout` - فرآیند تکمیل خرید
- `/account` - داشبورد حساب کاربری
- `/account/orders` - لیست سفارش‌ها
- `/account/settings` - تنظیمات حساب کاربری
- `/blog` - لیست مقالات
- `/blog/[slug]` - صفحه جزئیات مقاله
- `/search` - جستجوی محصولات

## نکات مهم

### داده‌های Mock

این پروژه از داده‌های استاتیک (Mock Data) استفاده می‌کند. تمام داده‌ها در پوشه `src/data/` قرار دارند و می‌توانند به راحتی با یک CMS یا API واقعی جایگزین شوند.

### مدیریت State

سبد خرید با Zustand مدیریت می‌شود و در localStorage ذخیره می‌شود تا در بازدیدهای بعدی حفظ شود.

### SEO

تمام صفحات دارای metadata مناسب برای SEO هستند و از Open Graph و Twitter Cards پشتیبانی می‌کنند.

### RTL Support

پروژه به طور کامل از راست به چپ (RTL) پشتیبانی می‌کند و برای زبان فارسی بهینه شده است.

## توسعه بیشتر

این پروژه به گونه‌ای طراحی شده است که به راحتی قابل توسعه باشد:

- **اتصال به Backend**: می‌توانید توابع در `src/data/` را با API calls جایگزین کنید
- **CMS Integration**: می‌توانید از CMS هایی مانند Contentful یا Sanity استفاده کنید
- **Authentication**: می‌توانید سیستم احراز هویت اضافه کنید
- **Payment Gateway**: می‌توانید درگاه پرداخت واقعی اضافه کنید

## لایسنس

این پروژه یک پروژه دمو است و برای استفاده در پورتفولیو طراحی شده است.

## تماس

برای سوالات یا پیشنهادات، لطفاً از طریق Issues در repository تماس بگیرید.

---

**نکته**: این یک پروژه دمو است و برای نمایش قابلیت‌های یک فروشگاه اینترنتی مدرن طراحی شده است.
